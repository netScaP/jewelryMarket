extends ./layout

block styles
	link(rel='stylesheet', href='/stylesheets/admin/singleProduct.css')

block content
	a(href='/admin/products') Back
	form(method="POST", @submit.prevent="submit")
		div.singleProduct
			div.left
				div.mainImage
					img(:src="product.imagePath[0]", alt="Empty")
				div.thumbnails
					img(v-for="image in product.imagePath", :src="image", alt="")
				input(v-for="image in product.imagePath", type="url", :value="image", v-model="image", placeholder="Ссылка на изображение").imagePath
				div.imagePathAdd(@click='addElement("imagePath")') +
			div.right
				div.prodDesc
					input(type="text", :value="product.title", v-model="product.title", placeholder="Название товара").title
					input(type="text", :value="product.type", v-model="product.type", placeholder="Тип товара").type
					div.priceDiv
						input(type="number", :value="product.price", v-model="product.price", placeholder="Цена").price
						span $
					div.size
						span Size(s): 
						input(v-for="oneSize in product.size", type="text", v-model="oneSize", :value="oneSize", class="sizeArr")
						div.sizeArrAdd(@click='addElement("size")') +
					div.selectQuantity
						p New Quantity:
						input(type="number", :value="product.quantity", v-model="product.quantity")
					div.description
						input(type="text", :value="product.description", v-model="product.description", placeholder="Описание товара")
		div.partDesc
			p Product Specifications:
			div.specific
				div.onePart(v-for="(val, index) in product.partDesc")
					div.ind
						input(type="text", :value="index", v-model="index", placeholder="Тип")
					div.value
						input(type="text", :value="val", v-model="val", placeholder="Кольцо")
				div.onePartAdd(@click='addElement("onePart")') +
		span Sale: 
		input(type="number", :value="product.sale", v-model="product.sale", placeholder="Скидка")
		br
		span AddInfo: 
		input(v-if="product.addInfo", type="text", v-model="product.addInfo", :value="product.addInfo", placeholder="Дополнительная информация")
		input(v-else, type="text", placeholder="Дополнительная информация", v-model="product.addInfo")
		br
		input(type="hidden", :value="product.unique", ref="unique")
		br
		button(type="submit", @click="submitClick()") UPDATE Product
block scripts
	script(src="../../javascripts/admin.js")